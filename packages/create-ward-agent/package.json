{
  "name": "create-ward-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Scaffolder for a WardAgentBase-derived Foundry project — `pnpm create ward-agent <name>`",
  "type": "module",
  "bin": {
    "create-ward-agent": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/index.js",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "cac": "^6.7.14",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@ward/sdk": "workspace:*",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
