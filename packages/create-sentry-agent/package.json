{
  "name": "create-sentry-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Scaffolder for a SentryAgentBase-derived Foundry project — `pnpm create sentry-agent <name>`",
  "type": "module",
  "bin": {
    "create-sentry-agent": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/index.js",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "cac": "^6.7.14",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@sentry-somnia/sdk": "workspace:*",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
